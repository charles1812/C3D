#include "libft.h"
#include <stdlib.h>

void	*ft_calloc(size_t nmemb, size_t size)
{
	void	*ptr;

	ptr = (void *)malloc(nmemb * size);
	if (ptr != NULL)
		ft_bzero(ptr, size * nmemb);
	return (ptr);
}
